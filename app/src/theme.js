import { css } from 'styled-components'

const theme = {
  colors: {
    text: "#444444",
    lightGrey: "#CCC",
    movify: "#F64D35",
  }
}

theme.colors.main = theme.colors.movify

export default theme
